#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @k: input as an integer
 * Return: the absolute values of int
 */

int _abs(int k)
{
	if (k >= 0)
	{
		return (k);
	}
	else
	{
		return (-k);
	}
}
